/* newPage.wxss
.container {
  padding: 20px;
}

.form-item {
  margin-bottom: 20px;
}

label {
  font-weight: bold;
  margin-right: 10px;
}

.picker {
  border: 1px solid #ddd;
  padding: 10px;
  border-radius: 5px;
}

/* 禁用状态的样式 */
.disabled-input {
  color: #999; /* 文字颜色变浅 */
  opacity: 0.7; /* 整体透明度降低 */
  pointer-events: none; /* 禁止点击 */
}
